dist/
.astro/
node_modules/
package-lock.json
public/fonts/
# Agent-local settings, untracked and never part of a build. Prettier reads the
# working directory rather than the index, so without this the pre-push gate
# fails on a file CI cannot see, which blocks a push for a reason that does not
# exist in the repository.
.claude/
